[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA-Fallback (optional, fuer Phaser irrelevant aber gut zu haben)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
