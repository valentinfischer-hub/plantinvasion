# Batch 7 Run 9 — Network-Error-Handling

**Datum:** 2026-04-30
**Status:** GRÜN
**Feature:** B7-R9

## Änderungen

### src/services/supabase.ts (erweitert)

**isOfflineMode() / setOfflineMode(v, reason):**
- Offline-Mode-Flag mit Timestamp
- Event-Listener: onNetworkError(cb) / onNetworkRecovery(cb)
- setOfflineMode ruft alle Listener auf
- offlineDuration(): ms seit Offline-Aktivierung

**withRetry(fn, maxAttempts, label):**
- Ruft fn bis zu maxAttempts-mal auf
- Exponential backoff: 500ms → 1000ms → 2000ms
- Promise.race mit SUPABASE_TIMEOUT_MS (5000ms)
- Bei dauerhaftem Fehler: setOfflineMode(true, reason)
- Bei Erfolg: setOfflineMode(false) wenn vorher offline

**_resetSupabaseCacheForTest erweitert:**
- Räumt auch _offlineMode, _offlineSince und Listener-Sets auf

### src/scenes/OverworldScene.ts

**Retry-Toast:**
- onNetworkError → showToast(..., 'error') mit gekürztem Reason (40 Zeichen)
- onNetworkRecovery → showToast(..., 'success')

### src/services/__tests__/supabase.test.ts

- 6 neue Tests für Offline-Mode, setOfflineMode, onNetworkError, withRetry

## Gates

- TS: alle Typen korrekt, keine any
- Vitest: neue Tests werden erwartet zu passen
- Console-Zero: kein console.log (setOfflineMode ist silent)
