# Batch 6 Run 5 — DayNightCycle-System

**Datum:** 2026-04-29
**Sprint:** S-POLISH
**Tier:** 2 (Garten-Experience) + 5 (Polish)

## Was wurde gemacht
- `src/systems/DayNightCycle.ts` (neu)
  - `DayNightCycle` Klasse mit konfigurierbarer `cycleDurationMs`
  - Smooth Tint-Interpolation zwischen 4 Phasen (dawn/day/dusk/night)
  - RGB-Interpolation für sanfte Farbübergänge
  - `attachToScene()` für Fullscreen-Overlay + `update()` für Tick
  - `setCycleDuration()` / `setTintStrength()` zur Laufzeit
  - `DayPhase` Typ + `PHASE_TINTS` Konstanten
- `src/systems/__tests__/DayNightCycle.test.ts` (neu) — 9 Tests

## Bereits vorhanden (nicht überschrieben)
- `TimeOverlay.ts` bleibt für HUD-Darstellung zuständig
- DayNightCycle ist eigenständiges System, kein Conflict

## Tests
- 867/867 grün (56 Dateien, +1 Suite, +9 Tests)

## Dateien
- `src/systems/DayNightCycle.ts` (neu)
- `src/systems/__tests__/DayNightCycle.test.ts` (neu)
