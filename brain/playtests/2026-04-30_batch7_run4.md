# Batch 7 Run 4 — Achievement-Unlock-Animation

**Datum:** 2026-04-30
**Status:** GRÜN
**Feature:** B7-R4

## Änderungen

### src/data/achievements.ts
- `tier?: 'bronze' | 'silver' | 'gold'` zum `AchievementDef`-Interface hinzugefügt
- first-bloom: `tier: 'bronze'`, pristine-grower: `tier: 'silver'`

### src/scenes/OverworldScene.ts (showAchievementToast)

**Slide-In von oben rechts:**
- `startX = (cam.width + W) / z` — Toast startet ausserhalb des Viewports
- `targetX = (cam.width - W/2 - 12) / z` — oben rechts, 12px Abstand zum Rand
- Tween 350ms `Back.Out` — springt leicht über

**Bronze/Silber/Gold-Icon:**
- gold → `★` (0xffd700), silver → `◆` (0xc0c0c0), bronze → `■` (0xcd7f32)
- undefined/default → `■` (bronze)

**Auto-Dismiss 4s:**
- Slide-Out nach rechts: 600ms Cubic.In + alpha 0 nach 4s delay

## Gates

- TS: `def.tier` ist optional → no-error mit `?.`
- Vitest: kein Logic-Change
- Console-Zero: OK
